/**
 * Configuración de entorno para producción
 * 
 * Para configurar la URL base de la API en producción:
 * 1. Modifica el valor de apiUrl antes de hacer el build de producción
 * 2. O usa variables de entorno del sistema durante el build
 */
export const environment = {
  production: true,
  apiUrl: 'https://humble-space-parakeet-w495jvqw69wcgp45-8000.app.github.dev/'
};
